# Test-only dependencies for ProgCheck
pytest>=7.4
pytest-asyncio>=0.23
pytest-cov>=4.1      # coverage reports: pytest --cov=src --cov-report=html
httpx>=0.27          # async test client (optional)
requests>=2.31
robotframework>=7.0
robotframework-requests>=0.9
# Frontend tests use: npm test (Jest + React Testing Library, pre-installed via CRA)
